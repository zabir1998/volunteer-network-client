import React from 'react';
import Header from '../Header/Header';

const Admin = () => {
    return (
        <div>
            <Header></Header>
            <h2>This is admin area</h2>
        </div>
    );
};

export default Admin;